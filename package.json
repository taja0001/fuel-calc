{
  "name": "fuel-calc-dev",
  "private": true,
  "type": "module",
  "description": "Dev tooling only — the app itself (index.html, sw.js, the pipeline scripts) has no dependencies and no build step. Playwright drives the real app in a real browser for the tests.",
  "scripts": {
    "lint": "node --check scripts/build-prices.mjs && node --check scripts/validate-prices.mjs && node scripts/check-app.mjs",
    "test": "node --test"
  },
  "devDependencies": {
    "playwright": "^1.54.0"
  }
}
