# Playwright MCP scratch output (screenshots, snapshots, console logs)
.playwright-mcp/

# dev tooling only — the app has no dependencies
node_modules/

# private working notes — handoffs, scratch thinking; never pushed
notes/

# Claude Code's local settings — saved permission answers, personal to this Mac
.claude/
